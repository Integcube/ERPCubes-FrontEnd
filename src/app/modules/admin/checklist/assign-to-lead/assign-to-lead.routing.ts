import { Routes } from "@angular/router";
import { AssignToLeadComponent } from "./assign-to-lead.component";


export const CreateChecklistRouting: Routes = [
    {
        path: '',
        component: AssignToLeadComponent,

    },
];